// Shared by the API and the UI. Managing categories needs the
// expense_categories permission (lib/permission-rules.ts).

export const EXPENSE_CATEGORY_NAME_MAX_LENGTH = 100
export const EXPENSE_CATEGORY_DESCRIPTION_MAX_LENGTH = 500
